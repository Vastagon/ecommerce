generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model items {
  id        String @id
  name      String
  imagePath String
  rating    Int
  price     Int
}

model users {
  id       String @id
  username String
}
